/* Kairali Trails — one-time config.
   Paste your Supabase anon (public) key between the quotes to switch ON
   live reviews + video testimonials across the ENTIRE site (one edit). */
window.KT_SB_KEY = '';
